// World-convention constant: built-in table primitives are authored with
// their top surface at local y=0, and the Table entity's transform is locked
// at world origin, so the play surface plane is always world y=0 regardless
// of uniform scale.
export const TABLE_SURFACE_Y = 0;
